function createTitle(title) {
  return `The ${title}`; 
};

createTitle();


var character = { 
  name: 'Gaston', 
  age: 35, 
  pronouns: 'they/them'
};

function buildMainCharacter(name, age, pronouns) {
  return {
    name: name, 
    age: age, 
    pronouns: pronouns
  }
};

buildMainCharacter();


var reviews = [];

function saveReview(review) {


saveReview();


module.exports = {
  createTitle,
  buildMainCharacter,
  saveReview,
  // calculatePageCount,
  // writeBook,
  // editBook
}

